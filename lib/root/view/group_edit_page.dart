import 'package:flutter/material.dart';

class GroupEditPage extends StatelessWidget {
  const GroupEditPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
